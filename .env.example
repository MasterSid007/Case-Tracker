# Server port (default: 3456)
PORT=3456

# Email for Web Push VAPID identification (default: admin@example.com)
VAPID_EMAIL=admin@example.com

# VAPID keys for Web Push notifications (auto-generated on first run if not set)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=
